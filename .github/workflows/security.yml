name: Security-Check

on:
  push:
    branches: main

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Bandit Security test
        run: |
          pip install bandit
          bandit -r app/

